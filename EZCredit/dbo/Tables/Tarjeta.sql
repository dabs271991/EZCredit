CREATE TABLE [dbo].[Table1]
(
	[Id] INT NOT NULL PRIMARY KEY, 
    [Codigo] INT NOT NULL, 
    [Tipo] VARCHAR(50) NOT NULL, 
    [Numero] INT NOT NULL, 
    [Vencimiento] DATE NOT NULL, 
    [CVV] INT NOT NULL
)
